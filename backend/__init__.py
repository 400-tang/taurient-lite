"""Taurient Lite 的 Web 后端。

跟 `taurient_lite/` 那个零依赖的静态生成流水线是两个独立的东西：
那边负责「每天生成一次成品页面」，这里负责「有人点开浏览器时，
现场再做点什么」。两者共用同一套业务逻辑（`taurient_lite` 包），
这个后端只是在外面包一层 HTTP，不重新实现任何抓取或渲染逻辑。

这个包依赖 FastAPI 和 uvicorn，是这个仓库里唯一需要装第三方库的
地方——`backend/requirements.txt` 只管这里，跟每日生成流水线的
零依赖原则不冲突，也不影响云端定时任务（它完全用不到这个包）。
"""
